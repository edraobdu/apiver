version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    install:
      - pip install uv
      - uv sync --group docs
    build:
      html:
        - uv run mkdocs build --strict --site-dir $READTHEDOCS_OUTPUT/html
